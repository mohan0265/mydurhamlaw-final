// src/components/FloatingAWY.tsx
'use client';

// Temporary no-op to unblock the app load.
// The AWY panel (AWYWidget) still works via its own launcher/button.
// We'll re-introduce the premium heart launcher once the app is stable.
export default function FloatingAWY() {
  return null;
}
